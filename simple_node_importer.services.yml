# config.factory helps us read data out of the config file in the "install" directory.
services:
  snp.get_content_types:
    class: Drupal\simple_node_importer\Services\GetContentTypes
    arguments: ["@config.factory"]